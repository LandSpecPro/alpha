<%= render 'layouts/sidebar' %>

<div class="row-fluid">
	<div class="span4 offset1">
		<%= form_for @user do |f| %>
			<div class="widget-box" style="min-width: 350px;">
				<div class="widget-header header-color-blue">
					<h5 class="bigger lighter"><i class="icon-user"></i> Company Logo</h5>
					<div class="widget-toolbar">
						<button type="submit" class="btn btn-mini btn-inverse no-radius white"><i class="icon-save"></i> Update</button>
					</div>
				</div>

				<div class="widget-body">
					<div class="widget-main no-padding">
						<table id="table-company-logo" class="table table-bordered">
							<tbody>
								<tr>
									<td >
										<ul class="ace-thumbnails">
											<li><div style="min-height: 100px; min-width: 100px;">
												<%= image_tag @user.get_logo.url(:thumb) %>
												</div>

											</li>
										</ul>
									</td>
									<td style="vertical-align:middle;">
										<%= f.fields_for :bus_vendor do |f2| %>
											<%= f2.file_field :logo %>
											<%= f2.hidden_field :id, :value => @user.bus_vendor.id %>
										<% end %>
									</td>
								</tr>
							</tbody>
						</table>
					</div>
				</div>
			</div>
				<% end %>
		</div>
	</div>
	<div class="row-fluid">
		<div class="span4 offset1">
			<%= render 'layouts/accountinfo' %>
		</div>
	</div>
<%= render 'layouts/sidebarend' %>