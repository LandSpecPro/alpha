<!-- THIS IS A TEST PAGE -->

<div class="container">
  <div class="hero-unit">
    <% @vendors.each do |vendors| %>
    	<%= image_tag vendors.logo.url(:thumb) %>
    <% end %>
  </div>
</div>