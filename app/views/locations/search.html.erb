<%= render 'layouts/sidebar' %>

<% if @locations.nil? and @otherlocations.nil? %>
	<%= render 'search' %>
	<%= render 'searchdefaulthighlight' %>
<% else %>
	<%= render 'search' %>
	<div class="row-fluid">
		<div class="span12">
			<% if @locations.count == 0 and @otherlocations.count == 0 %>
				<div class="center"><br/>
					<h3>0 locations were found!</h3>
				</div>
			<% else %>
				<%= render 'searchresultlist' %>
			<% end %>
		</div>
	</div>
<% end %>

<%= render 'layouts/sidebarend' %>


<script type="text/javascript">

	
	$(applyChosenStyle());
	$(applyColorbox());
	$(applyPopovers());

</script>