<!-- This page is just a placeholder at the moment in case more addons are added later. 
	For not it is only inventory, so that is the page that is rendered. -->
	
<%= render 'locations/layouts_edit/addons_inventory_manage' %>
